v1.5.0 -- TBD
* disable paging
* don't deploy test resources (like entire procfile tree)
* fix missing resource files in deployment
* 8145: hubmoni: update documentation of JSON quantities
* 8147: hubmoni: add CWD reporting

v1.4.9 -- 2 May 2017
* increase moni time from 1 to 2 minutes
* catch AttributeErrors if proc files go haywire
* enable paging
* remove hub name from alert text (it's in description)
* add alert grace period after reboot
* user alert needs a description
* add alert grace period after reboot
* remove emails from user alerts
* parallelize fab rollout

v1.4.8 -- 30 January 2017

* inital SPS deployment after project rework
 